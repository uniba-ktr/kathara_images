name: Docker Image CI

env:
  REGISTRY: ghcr.io

on:
  push:
    branches:
      - main
      - actions
  pull_request:
    branches:
      - main

jobs:

  build:
    runs-on: ubuntu-latest
    steps:

    - name: Set time
      run: echo "BUILD_DATE=$(date +%s)" >> $GITHUB_ENV
    - name: downcase REPO
      run: echo "REPO=${GITHUB_REPOSITORY,,}" >> ${GITHUB_ENV}
    - name: Checkout
      uses: actions/checkout@v3.4.0
    - name: Build the base images
      run: docker build base_images/ --dockerfile base_images/alpine.dockerfile --build-arg BUILD_DATE=${{ env.BUILD_DATE }} --build-arg VCS_REF=$(shell git rev-parse --short HEAD) --build-arg VCS_URL=$(shell git config --get remote.origin.url) --tag ${{ env.REGISTRY }}/${{ env.REPO }}:alpine-${{ env.BUILD_DATE }}
    
